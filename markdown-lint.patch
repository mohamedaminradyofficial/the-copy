diff --git a/backend/MCP_SERVER_README.md b/backend/MCP_SERVER_README.md
index 1234567..abcdefg 100644
--- a/backend/MCP_SERVER_README.md
+++ b/backend/MCP_SERVER_README.md
@@ -7,7 +7,7 @@

-### Tools
+### Tools

-- **Addition Tool** (`add`): Adds two numbers together
+
+- **Addition Tool** (`add`): Adds two numbers together
 - **Subtraction Tool** (`subtract`): Subtracts one number from another
 - **Echo Tool** (`echo`): Returns the input text

@@ -12,7 +12,7 @@

-### Resources
+### Resources

-- **Greeting Resource** (`greeting://{name}`): Dynamic greeting generator
+
+- **Greeting Resource** (`greeting://{name}`): Dynamic greeting generator

 Each greeting is generated on-demand and can be customized based on the name parameter.

@@ -18,7 +18,7 @@

-### Development Mode (with auto-reload)
+### Development Mode (with auto-reload)

 ```bash
 cd backend
@@ -24,7 +24,7 @@

-### Production Mode
+### Production Mode

 ```bash
 cd backend
@@ -41,7 +41,7 @@

-### 1. MCP Inspector
+### 1. MCP Inspector

 ```bash
 npx @modelcontextprotocol/inspector node build/mcp-server.js
@@ -47,7 +47,7 @@

-### 2. Claude Code
+### 2. Claude Code

 Add to your Claude Code configuration:

@@ -52,7 +52,7 @@

-### 3. VS Code
+### 3. VS Code

 Install the MCP extension and configure it to use this server.

@@ -57,7 +57,7 @@

-### 4. Cursor
+### 4. Cursor

 Similar to VS Code, install MCP support and point it to this server.

@@ -63,7 +63,7 @@

-1. Ask the AI agent to add two numbers using the `add` tool
+
+1. Ask the AI agent to add two numbers using the `add` tool
 2. Request a greeting using the `greeting` resource
 3. Echo back some text using the `echo` tool

@@ -95,7 +95,7 @@

-1. Add more tools in `mcp-server.ts` using `server.registerTool()`
+
+1. Add more tools in `mcp-server.ts` using `server.registerTool()`
 2. Add resources using `server.registerResource()`
 3. Add prompts using `server.registerPrompt()`

@@ -101,7 +101,7 @@

-- Session management
+
+- Session management
 - Persistent storage
 - Authentication
diff --git a/.idx/airules.md b/.idx/airules.md
index 1234567..abcdefg 100644
--- a/.idx/airules.md
+++ b/.idx/airules.md
@@ -13,7 +13,7 @@

-### General
+### General

-- **Language:** Use modern JavaScript (ES6+) or TypeScript, depending on the project's configuration.
+
+- **Language:** Use modern JavaScript (ES6+) or TypeScript, depending on the project's configuration.
 - **Code Quality:** Write clean, maintainable code. Follow SOLID principles.
 - **Comments:** Add comments where necessary, especially for complex logic.
 - **Testing:** Encourage writing unit tests and integration tests.
@@ -19,7 +19,7 @@

-### Node.js & Express Specific
+### Node.js & Express Specific

-- **Security:**
+
+- **Security:**
-    - **Secrets Management:** Never hard-code secrets. Use environment variables (and a `.env` file) for all sensitive information.
-    - **Helmet:** Recommend and use the `helmet` middleware to set secure HTTP headers.
-    - **Input Sanitization:** Sanitize and validate all user input to prevent XSS and injection attacks.
-    - **Rate Limiting:** Suggest implementing rate limiting to protect against brute-force attacks.
+  - **Secrets Management:** Never hard-code secrets. Use environment variables (and a `.env` file) for all sensitive information.
+  - **Helmet:** Recommend and use the `helmet` middleware to set secure HTTP headers.
+  - **Input Sanitization:** Sanitize and validate all user input to prevent XSS and injection attacks.
+  - **Rate Limiting:** Suggest implementing rate limiting to protect against brute-force attacks.
 - **Code Organization:**
-    - **Modular Design:** Organize the application into logical modules. Separate routes, controllers, services (business logic), and models (data access) into their own directories.
-    - **Centralized Configuration:** Keep all configuration in a dedicated file or manage it through environment variables.
+  - **Modular Design:** Organize the application into logical modules. Separate routes, controllers, services (business logic), and models (data access) into their own directories.
+  - **Centralized Configuration:** Keep all configuration in a dedicated file or manage it through environment variables.
 - **Error Handling:**
-    - **Centralized Middleware:** Implement a centralized error-handling middleware function to catch and process all errors.
-    - **Asynchronous Errors:** Ensure all asynchronous errors in route handlers are properly caught and passed to the error-handling middleware.
+  - **Centralized Middleware:** Implement a centralized error-handling middleware function to catch and process all errors.
+  - **Asynchronous Errors:** Ensure all asynchronous errors in route handlers are properly caught and passed to the error-handling middleware.
 - **Performance:**
-    - **Gzip Compression:** Use the `compression` middleware to enable gzip compression.
-    - **Caching:** Recommend caching strategies for frequently accessed data.
-    - **Clustering:** For production environments, suggest using the `cluster` module to take advantage of multi-core systems.
+  - **Gzip Compression:** Use the `compression` middleware to enable gzip compression.
+  - **Caching:** Recommend caching strategies for frequently accessed data.
+  - **Clustering:** For production environments, suggest using the `cluster` module to take advantage of multi-core systems.
 - **Logging:** Use a logging library like `winston` or `morgan` for structured logging.
 - **Middleware:** Be explicit about the order of middleware. Place error-handling middleware last.
