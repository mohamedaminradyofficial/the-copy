# اختبارات مكون CreativeWritingStudio

## نظرة عامة
يحتوي هذا المجلد على اختبارات الدخان (Smoke Tests) للمكون `CreativeWritingStudio`.

## ملف الاختبار
- `CreativeWritingStudio.test.tsx` - اختبار الدخان الأساسي للمكون

## أنواع الاختبارات
- **اختبار العرض (Render Test)**: التأكد من أن المكون يتم عرضه بدون أخطاء
- **اختبار الحالة الأولية (Initial State Test)**: التأكد من أن الحالة الأولية صحيحة
- **اختبار التصدير (Export Test)**: التأكد من أن التصدير يعمل بشكل صحيح
- **اختبار العناصر (Elements Test)**: التأكد من وجود عناصر الواجهة المطلوبة

## العناصر المختبرة
✅ **العنوان الرئيسي**: `🎨 استوديو الكتابة الإبداعية`  
✅ **التنقل**: الرئيسية، مكتبة المحفزات، المحرر، الإعدادات  
✅ **الرسالة الترحيبية**: `مرحباً بك في عالم الإبداع!`  
✅ **الأزرار**: `ابدأ الكتابة` وأزرار التنقل  
✅ **التصدير**: التأكد من أن المكون يُصدر بشكل صحيح  
✅ **الخصائص**: التأكد من قبول الخصائص الأولية  
✅ **إعادة العرض**: التأكد من إمكانية إعادة عرض المكون  

## التقنيات المستخدمة
- **Testing Library**: لاختبار React
- **Vitest**: إطار الاختبارات 
- **Mocking**: لمحاكاة المكونات الفرعية والتبعيات

## كيفية تشغيل الاختبارات

### تشغيل جميع الاختبارات
```bash
cd frontend
npm run test
```

### تشغيل اختبار واحد محدد
```bash
npx vitest run "src/app/(main)/arabic-creative-writing-studio/__tests__/CreativeWritingStudio.test.tsx"
```

### تشغيل الاختبارات في وضع المراقبة
```bash
npm run test:watch
```

### عرض تغطية الكود
```bash
npm run test:coverage
```

## التبعيات المحاكاة (Mocked Dependencies)
- `PromptLibrary`: مكون مكتبة المحفزات
- `WritingEditor`: مكون محرر الكتابة
- `SettingsPanel`: مكون لوحة الإعدادات
- `GeminiService`: خدمة الذكاء الاصطناعي
- مكونات واجهة المستخدم (Button, Card, Alert, Dialog)

## مثال على الاختبار
```typescript
it('يجب أن يتم عرض المكون بدون استثناءات', () => {
  expect(() => {
    render(<CreativeWritingStudio />);
  }).not.toThrow();
});
```

## حالة الاختبار
✅ **مكتمل**: تم إنشاء جميع اختبارات الدخان المطلوبة  
✅ **قابل للتشغيل**: جميع الاختبارات مُعدة بشكل صحيح  
✅ **شامل**: يغطي جميع المتطلبات الأساسية  

## الملاحظات
- يستخدم الاختبار vitest بدلاً من jest
- يتم محاكاة جميع التبعيات الخارجية
- الاختبارات مكتوبة بالعربية للوضوح
- يدعم React 18 و TypeScript