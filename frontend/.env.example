# ===================================================================
# Environment Configuration Template for The Copy (النسخة)
# ===================================================================
# Copy this file to .env.local and fill in the required values
# Never commit .env.local to version control
#
# For more details, see: src/env.ts
# ===================================================================

# -------------------------------------------------------------------
# Runtime Environment
# -------------------------------------------------------------------
# Options: development, production, test, staging
NODE_ENV=development

# Options: development, staging, production
NEXT_PUBLIC_APP_ENV=development

# -------------------------------------------------------------------
# AI/ML Services - Gemini API
# -------------------------------------------------------------------
# Get your API keys from: https://makersuite.google.com/app/apikey
# IMPORTANT: Keep these secret! Never expose to client-side code

# Staging environment API key (used for development and staging)
GEMINI_API_KEY_STAGING=

# Production environment API key (used only in production)
GEMINI_API_KEY_PROD=

# -------------------------------------------------------------------
# Error Tracking - Sentry
# -------------------------------------------------------------------
# Get your Sentry configuration from: https://sentry.io/settings/

# Sentry Data Source Name (DSN) - Server-side
SENTRY_DSN=

# Sentry Organization slug
SENTRY_ORG=

# Sentry Project name
SENTRY_PROJECT=

# Sentry Auth Token (for uploading source maps)
SENTRY_AUTH_TOKEN=

# Sentry DSN for client-side error tracking
NEXT_PUBLIC_SENTRY_DSN=

# -------------------------------------------------------------------
# Optional: Development Tools
# -------------------------------------------------------------------
# Enable bundle analyzer (set to 'true' to enable)
# ANALYZE=false

# -------------------------------------------------------------------
# CDN Configuration (اعدادات شبكة توزيع المحتوى)
# -------------------------------------------------------------------
# CDN URL for static assets (optional)
# If not set, assets will be served from the Next.js server
# Examples:
#   - Cloudflare CDN: https://cdn.yourdomain.com
#   - AWS CloudFront: https://d111111abcdef8.cloudfront.net
#   - Vercel Edge Network: Leave empty (handled automatically)
# NEXT_PUBLIC_CDN_URL=

# Enable CDN for static assets (set to 'true' to enable)
# NEXT_PUBLIC_ENABLE_CDN=false

# -------------------------------------------------------------------
# Redis Cache Configuration
# -------------------------------------------------------------------
# Option 1: Individual variables (recommended for Docker/local)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Option 2: Redis URL (recommended for cloud providers)
# REDIS_URL=redis://:password@host:6379
# Or without password: REDIS_URL=redis://localhost:6379

# -------------------------------------------------------------------
# Notes:
# -------------------------------------------------------------------
# 1. All NEXT_PUBLIC_* variables are exposed to the browser
# 2. Variables without NEXT_PUBLIC_ prefix are server-side only
# 3. Required variables in production:
#    - GEMINI_API_KEY_PROD
#    - NODE_ENV=production
#    - NEXT_PUBLIC_APP_ENV=production
# 4. For staging deployments:
#    - GEMINI_API_KEY_STAGING
#    - NODE_ENV=staging
#    - NEXT_PUBLIC_APP_ENV=staging
# 5. Redis is optional - if not configured, caching will be disabled
# ===================================================================
