# 📋 قائمة مهام تكامل استوديو هندسة الأوامر العربية

هذه قائمة مهام مفصلة لتكامل "استوديو هندسة الأوامر العربية" في مشروع Next.js 15 باستخدام App Router.

## 1️⃣ الإعداد والهيكلة (أولوية عالية)

- [ ] إنشاء بنية المجلدات الأساسية للمسار (route) الجديد: `frontend/src/app/(main)/arabic-prompt-engineering-studio/`.
- [ ] إنشاء ملفات المسار الأساسية داخل المجلد الجديد: `page.tsx`, `layout.tsx`, `loading.tsx`.
- [ ] نسخ الملفات المصدر (`app.js`, `index.html`, `style.css`) من `arabic-prompt-engineering-studio/` إلى مجلد مؤقت (staging) للبدء في عملية الترحيل.
- [ ] إنشاء ملف `types/index.ts` لتعريف الأنواع (Types) الخاصة بالاستوديو.

## 2️⃣ ترحيل وتسمية المكونات (أولوية عالية)

- [ ] تحويل محتوى `app.js` إلى مكون React رئيسي باسم `PromptEngineeringStudio.tsx` داخل مجلد `components`.
- [ ] تقسيم واجهة المستخدم من `PromptEngineeringStudio.tsx` إلى مكونات أصغر ومتخصصة:
    - [ ] `PromptEditor.tsx` (محرر الأوامر).
    - [ ] `EnhancedOutput.tsx` (عرض النتائج المحسنة).
    - [ ] `FrameworkSelector.tsx` (محدد أطر العمل).
    - [ ] `PromptMetrics.tsx` (مقاييس الأمر).
    - [ ] `TemplateLibrary.tsx` (مكتبة القوالب).
- [ ] التأكد من أن أسماء ملفات المكونات تتبع صيغة `PascalCase` وأسماء المجلدات تتبع `kebab-case`.

## 3️⃣ التكامل مع التوجيه والتنقل (أولوية عالية)

- [ ] التأكد من أن `page.tsx` يعرض المكون الرئيسي `PromptEngineeringStudio` ويمكن الوصول إليه عبر المسار `/arabic-prompt-engineering-studio`.
- [ ] إضافة رابط (Link) في شريط التنقل الرئيسي (e.g., `components/ui/NavBar`) للانتقال إلى صفحة الاستوديو الجديدة.
- [ ] تحديد حدود الخادم والعميل (`"use server"` و `"use client"`) بشكل صحيح في المكونات الجديدة.

## 4️⃣ إدارة الحالة والخدمات (أولوية عالية)

- [ ] **إذا كان المشروع يستخدم Redux/Zustand:** إنشاء slice جديد `store/slices/arabicPromptStudio.ts` وإضافته إلى الـ store الرئيسي.
- [ ] **إذا كان المشروع يستخدم Hooks/Context:** إنشاء hook مخصص `hooks/useArabicPromptStudio.ts` لإدارة حالة الاستوديو.
- [ ] إنشاء خدمة `lib/gemini-service.ts` لتغليف استدعاءات Gemini API، مع إعادة استخدام أي تطبيق موجود إذا أمكن.

## 5️⃣ البيانات، المنطق، والأنواع (أولوية عالية)

- [ ] نقل البيانات الثابتة (قوالب، أطر عمل) من `app.js` إلى `lib/prompt-data.ts`.
- [ ] نقل دوال التحليل (e.g., `analyzePromptQuality`) من `app.js` إلى `lib/prompt-analyzer.ts`.
- [ ] تعبئة ملف `types/index.ts` بجميع الأنواع اللازمة (e.g., `PromptType`, `FrameworkType`) وضمان التوافق مع TypeScript.

## 6️⃣ الأنماط، التنسيق، ودعم RTL (أولوية متوسطة)

- [ ] تحويل `style.css` إلى `styles/prompt-studio.module.css` أو استخدام فئات Tailwind CSS حسب معايير المشروع.
- [ ] استبدال متغيرات CSS الخام بقيم من نظام التصميم (design tokens) الخاص بالمشروع.
- [ ] التأكد من أن التخطيط يدعم RTL بشكل كامل إما عن طريق `dir="rtl"` في `layout.tsx` أو باستخدام `RtlProvider` الخاص بالمشروع.
- [ ] التأكد من أن التصميم متجاوب (responsive) ومتوافق مع سمات الألوان (light/dark mode) للمشروع.

## 7️⃣ المسارات المستعارة والتحقق (أولوية متوسطة)

- [ ] تحديث جميع مسارات الاستيراد (imports) لاستخدام المسارات المستعارة للمشروع (e.g., `@/components/ui`).
- [ ] التحقق من عدم وجود تعارضات في الأنماط العامة (global styles) بعد دمج `style.css`.

## 8️⃣ إمكانية الوصول والتدويل (i18n) (أولوية متوسطة)

- [ ] إضافة سمات ARIA الأساسية ودعم التنقل عبر لوحة المفاتيح للمكونات التفاعلية.
- [ ] **إذا كان المشروع يستخدم i18n:** تسجيل جميع النصوص الثابتة في ملفات الترجمة (e.g., `locales/ar.json`).

## 9️⃣ الاختبارات وضمان الجودة (أولوية متوسطة)

- [ ] كتابة اختبارات الوحدات (Unit Tests) لدوال التحليل في `prompt-logic.test.ts`.
- [ ] كتابة اختبارات التكامل (Integration Tests) للتحقق من عرض الصفحة والتنقل في `route-navigate.test.ts`.
- [ ] إضافة اختبارات أساسية لإمكانية الوصول (Accessibility smoke tests).

## 🔟 الفحص النهائي والنشر

- [ ] تشغيل `tsc --noEmit` للتأكد من عدم وجود أخطاء TypeScript.
- [ ] تشغيل `pnpm lint --max-warnings=0` للتأكد من خلو الكود من مشاكل linting.
- [ ] تشغيل `pnpm test` للتأكد من نجاح جميع الاختبارات الجديدة.
- [ ] إنشاء Pull Request (PR) من فرع `feature/arabic-prompt-engineering-studio` مع وصف مفصل وخطوات الاختبار.